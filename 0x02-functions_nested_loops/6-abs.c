#include "main.h"
/**
 * _abs - function that computes the absolute value of an integer
 * @labs: checks the absolute value
 * Return: Always 0
 */
int _abs(int labs)
{
	if (labs < 0)
	return (-labs);
	else
	return (labs);
}
